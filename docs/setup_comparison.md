# Setup Comparison

Agni-v2 differs from the earlier pilot lineage in three ways:

1. It treats configuration as a first-class artifact rather than ad hoc script arguments.
2. It enforces a feature guard before training, with explicit leakage patterns and allowed feature namespaces.
3. It centers spatiotemporal validation and partial-supervision severity modeling in the package structure itself.
