__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
.venv/

# Built data and experiment artifacts are reproducible from configs; never commit them.
# Anchored to repo root so they do not match the src/agni/data/ source package.
/data/
/outputs/
